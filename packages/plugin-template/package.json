{
  "name": "@yaz/plugin-template",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Starter template for a yaz plugin. MIT so that what you build from it is yours to license.",
  "scripts": {
    "check": "tsc -p tsconfig.json",
    "lint": "echo \"(lint rules land with the plugin runtime in phase 3)\"",
    "test": "echo \"(no template tests yet)\""
  },
  "dependencies": {
    "@yaz/api": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
